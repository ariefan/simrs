ALTER TABLE `tarif_general`
MODIFY COLUMN `tarif_general_id`  varchar(10) NOT NULL FIRST ;

ALTER TABLE `tarif_paramedis`
MODIFY COLUMN `tarif_paramedis_id`  varchar(10) NOT NULL FIRST ;

ALTER TABLE `tarif_unitmedis`
MODIFY COLUMN `tarif_unitmedis_id`  varchar(10) NOT NULL FIRST ;

UPDATE tarif_unitmedis set tarif_unitmedis_id = concat('U',tarif_unitmedis_id);
UPDATE tarif_kelas set seq_no = concat('K',seq_no);

ALTER TABLE `tarif_kelas`
MODIFY COLUMN `seq_no`  varchar(10) CHARACTER SET latin1 COLLATE latin1_swedish_ci NOT NULL FIRST ;

ALTER TABLE `tarif_tindakan`
MODIFY COLUMN `tarif_tindakan_id`  varchar(10) NOT NULL FIRST ;

ALTER TABLE `tarif_inventori`
MODIFY COLUMN `seq_no`  varchar(10) NOT NULL FIRST ;

INSERT INTO `rsberau`.`asuransi` (`insurance_cd`, `insurance_nm`) VALUES ('MANDIRI', 'MANDIRI');


TAMBAH KOLOM RUANG_CD di kunjungan

ALTER TABLE `pasien`
ADD COLUMN `suku`  varchar(255) NULL AFTER `jenis_asuransi`,
ADD COLUMN `pekerjaan`  varchar(255) NULL AFTER `suku`;

ALTER TABLE `rsberau`.`rm_diagnosis` ADD COLUMN `kasus` ENUM('Baru','Lama') NULL AFTER `nama_diagnosis`;